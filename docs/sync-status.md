# Sync Status

Last synced from active Cursor locations on 2026-08-24. Last committed and pushed to GitHub on 2026-08-30 (PR #2, merge `e720b44`) — those two dates diverged for 5 weeks because the 2026-08-02 and 2026-08-24 sync passes were never actually committed to this repo; see the 2026-08-30 note below.

See `docs/handoff.md` for the current open work items and where the last session left off — check it first when resuming.

## Repository Location

- Local clone: `C:\Users\brian\cursor-agent-skills`
- GitHub: `https://github.com/bkjohn2018/cursor-agent-skills`

## Source Of Truth (Runtime)

- Skills: `C:\Users\brian\.cursor\skills`
- Commands: `C:\Users\brian\.cursor\commands`
- Workflow skills: `C:\Users\brian\.cursor\skills-cursor`
- Agents: `C:\Users\brian\.cursor\agents` (created 2026-07-11 to run the `build-governance-bundle` pilot; now the runtime source, same as skills/commands above)

## Distribution Package (This Repo)

- Skills: `skills/`
- Commands: `commands/`
- Workflow skills: `cursor-workflow-skills/`
- Agents: `agents/`

## Current Counts

| Area | Count |
|---|---:|
| Domain skills | 65 |
| Commands | 13 |
| Workflow skills | 13 |
| Agents | 5 (1 orchestrator + 3 specialists + 1 QA gate; pilot scope, `build-governance-bundle` only). Specialist renamed 2026-08-02: `data-quality-controls-agent` → `data-quality-agent`. |

## Sync Procedure

1. Copy all folders from `.cursor\skills` to `skills\` with overwrite.
2. Copy all `*.md` from `.cursor\commands` to `commands\` with overwrite.
3. Update `docs\inventory.md` if new packs were added.
4. Commit and push from `cursor-agent-skills`.

Note: `agents/` currently syncs in the opposite direction from the procedure above — it is authored in this
repo first, then copied to `.cursor\agents` (last done 2026-07-11, after dry-run testing and fixes). Revisit
this once agents are edited directly at runtime rather than in the distribution repo.

## Notes

- **2026-08-30 — repo brought back into sync with GitHub, and `pbip-build-verification` cross-validated against Microsoft's own toolkit.** Found at session start: this repo was still sitting at commit `56c85b3` (2026-07-25) on branch `cursor/agents-pilot-governance-bundle` — already merged to `main` via PR #1 — with every file listed in the two notes below still sitting as *uncommitted* working-tree changes, 5 weeks after the fact. Confirmed `git diff 56c85b3 origin/main` was empty before acting, then branched fresh off `origin/main`, committed, pushed, and opened PR #2 (merged by Brian as `e720b44`); local `main` fast-forwarded to match; both the stale pilot branch and the new working branch deleted (local + remote) — repo is down to a single `main`. Separately, Brian asked to test the `pbi_design_test` build (see 2026-08-24 note below) against Microsoft's actual `powerbi-authoring` CLI toolkit as a comparison exercise: `powerbi-report-author validate` caught a real, recurring defect — `definition.pbir` missing `$schema` again, with **zero edits or Desktop sessions in the 6 days since it was last fixed** on 2026-08-24, ruling out the "stripped on Desktop resave" theory. Fixed the same way (copied the correct schema URL from a known-good sibling PBIP), re-validated clean, confirmed both pages still render with no drift via Desktop screenshot compared against the 08-24 reference image. Net finding, worth encoding in `pbip-build-verification`'s own file next time it's touched: the two toolkits are complementary, not overlapping — MS's `validate`/render loop is structural/schema/rendering correctness and is a stated *prerequisite* this skill leans on rather than re-checks, so this skill alone would have shipped the build with the `$schema` regression still in it; conversely MS's toolkit has no concept of `report-spec.md` conformance (field bindings, sort policy, `color_map`) at all, which is this skill's entire reason to exist. A screenshot-review finding ("Fields here" appearing to overlap the insight card) was investigated by cropping the actual pixels rather than trusted at first glance, and turned out to be a misread — it's the normal Filters-pane empty-state text on the sidebar, not a defect.
- New domain skill added and synced on 2026-08-24: `pbip-build-verification` (Dashboard And Webspace category) — checks a built Power BI project (.pbip/TMDL/PBIR) against its approved design brief and reports spec-versus-build divergence (measure bindings, sort logic, conditional formatting, color tokens, semantic-model governance). Report-only by design, mirroring `governance-bundle-qa-agent`'s find-don't-fix separation. Grew out of a same-day pilot build (AdventureWorks `pbi_design_test`, via the newly-installed `powerbi-authoring` Claude Code plugin) that hit five real bugs no CLI validation step caught — all five are now encoded as a standing "Known-Unsafe Build Patterns" section: a TopN filter ordered by a DAX measure crashes Desktop (`SemanticQueryRewriter.rewriteOrderBy`), a conditional `dataPoint.fill` with no `dataViewWildcard` selector renders one flat color for every point instead of erroring, `isAvailableInMdx: false` on a `sortByColumn` target breaks the column, TMDL `///` descriptions must lead an object's properties not trail them, and comparing a `date`-typed source column to a `#datetime(...)` M literal throws only at refresh, not at validation. Named `pbip-build-verification` rather than joining the `dashboard-*` prefix because it's genuinely PBI/TMDL-specific, unlike that family's generic-web scope (`dashboard-frontend-implementation` is React-flavored — components, props, data contracts). Dry-run tested via Claude Code's Agent tool, two rounds: a content test (a fresh agent, given only the skill file, checked the AdventureWorks brief against a second, independently-built implementation of the same pilot and correctly found real uncorrected divergences — auto date/time never disabled, `ModelRegion` never split, no conditional formatting on the driver chart, near-miss hex values like `#2F6BFF` vs. the brief's `#1F5EFF` that a screenshot wouldn't catch — while correctly declining to guess a fix for the undocumented TopN-by-measure gap) found one real ambiguity (whether report theme JSON is in scope for the color-literal check), fixed by scoping that check explicitly to `visual.json` and excluding theme files unless the brief specifies theme-level tokens; a boundary/routing test against `dashboard-metric-semantics` and `dashboard-frontend-implementation`'s descriptions confirmed the skill's own `description` field carries its scope boundaries and routes correctly in isolation, no fix needed. Verified byte-identical between `.cursor\skills\pbip-build-verification\SKILL.md` and `skills\pbip-build-verification\SKILL.md`. The Analytical Product Design research/standard work this grew out of was deliberately *not* turned into its own skill — it was judged over-engineered in its governance apparatus (approval gates, MUST/SHOULD requirement IDs with evidence tags, exception YAML with approver/expiration fields) relative to this library's dashboard-family house style (~55-line skills: description, Workflow, Output Template, Quality Checklist, no gate/approval scaffolding) — the Standard is retained as a reference document to mine as practice matures, not a thing every build must satisfy.
- `pbip-build-verification` was rewritten the same day (still 2026-08-24) to target the schema the installed tooling actually produces, after closer reading of `powerbi-report-planning`/`powerbi-report-design` (the `powerbi-authoring` plugin's brief-generation half) showed they already emit a rigorous, approval-gated `_brief/report-spec.md` with an embedded `Design Brief:` YAML (`layout_contract`, `color_map`, per-placement `field_bindings`/`sort_policy`/`color_strategy`) — a stronger generator than initially credited, including Cleveland & McGill-grounded chart selection. The skill now diffs a build against that real schema instead of the Gate 0-2 brief format nothing installed actually generates, and explicitly distinguishes **build gaps** (build didn't implement what the spec says) from **brief gaps** (the spec's own schema can't express what the page needs — found one concrete case: `sort_policy` has no magnitude/absolute-value option, so a "ranked by contribution" page like today's YoY driver chart cannot be expressed by the brief at all). Known-Unsafe Build Patterns carried over unchanged and confirmed non-redundant against `powerbi-report-design`'s own gotchas/`color.md` (design owns the *decision* to conditionally format; PBIR *mechanics* like the `dataViewWildcard` selector are specified nowhere upstream, which is why this skill owns that check). Verified byte-identical between `.cursor\skills\pbip-build-verification\SKILL.md` and `skills\pbip-build-verification\SKILL.md` after the rewrite. **Re-tested against a real artifact the same day**: actually invoked `powerbi-report-planning` + `powerbi-report-design` (via the Skill tool) against the `pbi_design_test` build in retrofit mode, producing a real `pbi_design_test/_brief/report-spec.md` (self-checked against `report-planning`'s own acceptance checklist, passed). Re-ran the dry-run against it — correctly reproduced every gap the spec's own "Known Deltas" section already flagged (independently verified, not copied), and found one the spec missed: a KPI card's value/label colors are inverted relative to `color_map` and the stated signature, traced to two overlapping color rules (text-hierarchy vs. measure-direction) assigning the same two colors in opposite directions on a card specifically. Also caught and fixed a real Workflow gap this surfaced: `color_map` is measure-scoped only, so placements with no measure behind their color (a card's own value, plain textboxes) had nothing to check against — added an explicit fallback to the spec's "Design system summary" prose plus a brief-gap flag when a `color_map` entry is missing. Verified byte-identical after the fix.
- `pbip-build-verification` gained two more Known-Unsafe Build Patterns the same day (still 2026-08-24), found for real rather than anticipated, while building a second star-schema page (`Product` denormalized dimension + `Inventory Movements` periodic-snapshot fact, related to the existing conformed `Date` table) in the same `pbi_design_test` pilot: (1) **a measure and a hidden backing column sharing the exact same name in one table** — passed `powerbi-report-author validate` clean, then failed Desktop *load itself* (not render, not reload) with `The '<name>' measure cannot be created because a column with the same name already exists`; Brian caught this one before the diagnosis finished and correctly called it "a rookie mistake" — fixed by keeping the raw backing column at its source-cased name (`UnitsIn`) distinct from the friendly measure name (`Units In`); (2) **a visual-level `filterConfig` nested inside `visual` instead of as its sibling** — this one *was* caught immediately by `powerbi-report-author validate` (`/visual must NOT have additional properties (property: "filterConfig")`), so it's recorded as a schema-validation catch, not a load-time surprise like the first. Both added as standing checks; total Known-Unsafe Build Patterns count is now 7. A third finding from the same build — `powerbi-desktop reload` doesn't pick up semantic-model TMDL changes, only PBIR, requiring a full close/reopen — was deliberately *not* added to the skill, since it's a live-Desktop-session behavior invisible to the skill's static-file review scope; documented instead in the pilot's own `_brief/report-spec.md` implementation notes. Verified byte-identical between `.cursor\skills\pbip-build-verification\SKILL.md` and `skills\pbip-build-verification\SKILL.md` after the edit. The skill was then run for real (not a dry-run) against the finished two-page build — full report at `pbi_design_test/_brief/verification-report-2026-08-24.md` — and passed both pages clean: zero build gaps, four brief-schema gaps (the pre-existing magnitude-sort and signed/threshold-color gaps, now confirmed *recurring* on a second, independent measure/fact-table pair rather than one-off; a still-absent TopN cap; and one new gap, a visual-level attribute filter with no home in the placements schema). None of the four gaps were fixed — logged for a future session per Brian's own explicit deferral ("we can dig into that stuff tomorrow"), consistent with the skill's report-only, no-schema-invention design.
- Keep this repository at `C:\Users\brian\cursor-agent-skills` for visibility and easy access.
- Workflow skills in `cursor-workflow-skills/` should be compared separately against `.cursor\skills-cursor` when Cursor tooling skills change.
- `governance-project-delivery` was edited and synced out-of-band on 2026-08-02 (single-file targeted sync, not a full sync pass) — added a governed-vs-non-governed tailoring gate (PMBOK 7 §2.3.4/§3.4.3) so the skill no longer assumes every initiative is a governance workstream. Verified byte-identical between `.cursor\skills\governance-project-delivery\SKILL.md` and `skills\governance-project-delivery\SKILL.md` after the edit. Dry-run tested via Claude Code's Agent tool: a POC/demo request correctly exits to `discovery-analyst`; a real SOX-control rollout correctly proceeds to a Charter (no regression). See `docs/handoff.md` for full detail.
- Two new domain skills added and synced out-of-band on 2026-08-02, filling the Analyst-role gap identified alongside the `governance-project-delivery` work: `predictive-model-development` (six-stage predictive/statistical modeling workflow — framing, EDA, dimensionality reduction, model selection, validation, translation; CRISP-DM-aligned, Model Cards-style decision logging, Hyndman & Athanasopoulos-anchored forecasting definition) and `descriptive-pattern-analysis` (four-stage descriptive workflow — framing, technique, materiality/robustness, translation; also Hyndman-anchored for trend vs. a raw YTD total, with an explicit spurious-correlation/shared-trend check). Both dry-run tested via Claude Code's Agent tool against real scenarios (AP workload clustering; pipeline throughput trend and throughput/capex correlation) and boundary-tested for routing against `financial-variance-analysis`, `reconciliation-analytics`, and each other; each round found and fixed a real gap (grain-justification and segmentation-keyword ambiguity, then a forecasting-vs-scheduled-date ambiguity, in `predictive-model-development`; a spurious-correlation gap in `descriptive-pattern-analysis`). `financial-variance-analysis`'s description was also narrowed to predefined dimensions as part of the same disambiguation work, and `data-profiling` / `question-driven-data-projects` now carry forward references to both new skills. All files verified byte-identical between `.cursor\skills\` and `skills\` after each sync. See `docs/handoff.md` for full detail.
- `data-profiling` was reworked on 2026-08-02 after Brian flagged that only wiring it forward into the two new analytics skills conflated data-engineering/architecture profiling (onboarding a table, source readiness) with EDA-function profiling — a real risk, confirmed by `data-profiling` having zero cross-references in either direction before this session. Grounded in Bruce, Bruce & Gedeck's *Practical Statistics for Data Scientists* (EDA presupposes data already structured; profiling establishes that structure, it isn't EDA itself). Description and "Relationship to Other Skills" now fork engineering (`dimensional-modeling`, `data-model-requirements-and-quality`, `data-quality-controls`) vs. analytics (`descriptive-pattern-analysis`, `predictive-model-development`) explicitly. Reciprocal references added to all three engineering-side skills, with `data-quality-controls`'s phrased carefully after checking `build-governance-bundle`'s actual 12-phase pipeline (which chains straight from `metric-governance` into `data-quality-controls` with no profiling/quality-assessment step at all — a real, newly-found gap in that pipeline, flagged but not fixed). An initial claim that the governance-bundle pipeline already assessed data quality "elsewhere" was checked, found false, and corrected rather than left in place. All files verified byte-identical between `.cursor\skills\` and `skills\` after each edit. See `docs/handoff.md` items 11-13 for full detail.
- `build-governance-bundle`'s quality-assessment gap (flagged 2026-08-02, see `data-quality-controls` note above) was closed the same day: `data-quality-controls-agent` renamed to `data-quality-agent` and extended to own `data-quality-assessment` alongside `data-quality-controls`, applied in that order (matching the ordering already established in `commands/build-governance-bundle.md`'s fuller 18-section variant, which already had `data-quality-assessment` positioned after controls — checked before assuming an order, per standing "no assumptions" feedback). `skills/build-governance-bundle/SKILL.md`'s Orchestration Order grew from 12 to 13 skills and its Output Template from 13 to 14 sections. Dry-run tested (test domain: Meter Reading Data Governance) — the design change worked correctly on the first pass (two distinct outputs, correct renumbering, glossary cross-check covering both, QA gate deriving the new structure with no hardcoded count and catching two genuine unrelated issues), but the test also caught that the agent rename hadn't propagated to `governance-writing-agent.md`, `governance-bundle-qa-agent.md`, and a second stale count in `governance-bundle-orchestrator.md` — fixed and verified clean via a full grep sweep, not a second dry-run round. All files verified byte-identical between `.cursor\agents\`/`.cursor\skills\` and their distro copies after every edit. See `docs/handoff.md` items 14-18.
- `agents/` pilot (`build-governance-bundle` only) was dry-run tested via Claude Code's Agent tool on
  2026-07-11 (test domain: Treasury Cash Positioning) across two full rounds before being copied to
  `.cursor\agents`:
  - Round 1 found: RACI/GL-cash-tie-out conflation with the unowned daily reconciliation control; a
    metric-glossary self-check gap (a metric named with no exact-match glossary entry); role-name drift
    between phases (`Analyst`/`Cash Mgmt Analyst`/`Treasury Analyst` used for one role, an invented
    "Controller delegate"); 3 Output Template sections silently unproduced; no Bundle Inputs recap.
  - Fixes applied: orchestrator writes Bundle Inputs itself and checks template-section coverage against
    the live specialist roster; writing agent's self-check requires an exact-name glossary match, not a
    looser "traces to" relationship; foundation agent now produces a canonical roles list plus explicit
    disambiguation of confusable controls, which every later phase must reuse verbatim.
  - Round 2 re-test confirmed all of round 1's findings fixed, and the QA gate (working independently, not
    told what to look for) found two new/residual issues on its own: the hardcoded 3-section gap list was
    already stale (RACI and Issue Management also have no owning specialist), and a DQ rule introduced an
    undefined glossary term the same way the round-1 metric had. Both fixed: the orchestrator's section
    check is now general (derive coverage from the live roster, not a fixed list) rather than a hardcoded
    list, and `data-quality-controls-agent` now runs the same glossary cross-check discipline the writing
    agent has.
  - Pilot considered validated after round 2 — the QA gate demonstrated it catches real, non-repeated
    issues across two independent runs rather than rubber-stamping. Not re-tested a third time; further
    validation should happen through real use.
